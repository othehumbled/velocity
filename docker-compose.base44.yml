services:
  web:
    image: node:22-slim
    working_dir: /app/server
    volumes:
      - ./:/app
    command: sh -c "npm install --no-audit --no-fund && node --watch server.js"
    ports:
      - "3000:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
